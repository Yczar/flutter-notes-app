name: notes_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_rx_bloc: ^4.0.2
  rxdart: ^0.27.5
  rx_bloc: ^3.3.1
  connectivity: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  rx_bloc_generator:
  build_runner:
  

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

